# Astra prompt lineage

Astra was built from the exact reusable [shared comparison prompt](../shared-prompt.md). The shared file is preserved verbatim so future models can receive the same goal, source passage, creative direction, controls, refinement rubric, and completion conditions.

Run metadata and the transparent cost scenarios for this GPT-6 Astra execution are in [run-metadata.json](./run-metadata.json).
